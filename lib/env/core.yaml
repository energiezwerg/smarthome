env:

    core:

        version:
            type: str

        start:
            type: foo

        memory:
            type: num
            sqlite: init
            database: init

        threads:
            type: num
            sqlite: init
            database: init

        garbage:
            type: num
            sqlite: init
            database: init
